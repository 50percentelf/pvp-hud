package com.pvphud;

public enum SpecDisplay
{
	/** "SPEC 75" — label + number in the action strip. */
	LABELED,
	/** "75%" — number only, saves strip space. */
	NUMBER,
	/** Thin bar drawn across the bottom of the action strip background. */
	BAR
}
